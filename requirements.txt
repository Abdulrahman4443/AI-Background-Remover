# =========================================================
# AI Background Removal System — Python Requirements
# Covers: AI inference pipeline + FastAPI backend
# =========================================================

# ---------- Core Deep Learning ----------
torch==2.3.1
torchvision==0.18.1
# GPU builds: uncomment the line below and remove the torch/torchvision lines above
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.3.1+cu121
# torchvision==0.18.1+cu121

# ---------- AI Model: Segmentation ----------
# U²-Net, BiRefNet, RMBG-2.0 dependencies
transformers==4.44.2          # for RMBG-2.0 (Hugging Face) and BiRefNet
huggingface-hub==0.24.6
timm==1.0.9                   # backbone architectures used by BiRefNet
safetensors==0.4.4
einops==0.8.0                 # tensor ops used in BiRefNet

# ---------- Image Processing ----------
# opencv-contrib-python includes ximgproc (guided filter for better edge matting)
# Do NOT install opencv-python alongside this — they conflict.
opencv-contrib-python==4.10.0.84
Pillow==10.4.0
numpy==1.26.4
scikit-image==0.24.0          # edge refinement / mask post-processing
rembg==2.0.59                 # prebuilt segmentation pipeline (ISNet / U²-Net / BiRefNet)

# ---------- Alpha Matting ----------
pymatting==1.1.12             # closed-form alpha matting (used in postprocessing.py)

# ---------- Optimization / Inference ----------
# Install ONLY ONE of onnxruntime or onnxruntime-gpu depending on your hardware.
onnx==1.16.2
onnxruntime==1.19.2           # CPU-only inference — use this for local dev / Docker CPU
# onnxruntime-gpu==1.19.2     # GPU inference — uncomment and remove onnxruntime above

# ---------- Backend: FastAPI ----------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # required for file/image upload handling
pydantic==2.9.2
pydantic-settings==2.5.2

# ---------- Backend: MongoDB ----------
pymongo==4.8.0
motor==3.5.1                  # async MongoDB driver for FastAPI
beanie==1.26.0                # ODM built on Motor + Pydantic
dnspython==2.6.1              # required for mongodb+srv:// Atlas connection strings

# ---------- Backend: Auth / File / Utils ----------
python-jose[cryptography]==3.3.0   # JWT token generation and validation
passlib[bcrypt]==1.7.4             # password hashing
aiofiles==24.1.0                   # async file I/O for uploads
python-dotenv==1.0.1               # .env file loading
loguru==0.7.2                      # structured logging

# ---------- Backend: Storage ----------
boto3==1.35.23                # AWS S3 / Cloudflare R2 / MinIO (required when STORAGE_BACKEND=s3)

# ---------- AI Chat / Analysis (LLM providers) ----------
google-generativeai==0.8.3    # Gemini SDK (required when AI_PROVIDER=gemini)
openai==1.51.0                # OpenAI-compatible client — used for Groq provider

# ---------- Testing ----------
pytest==8.3.3
pytest-asyncio>=0.24.0
httpx==0.27.2                 # async HTTP client for testing FastAPI endpoints
